# Notebooks

Папка для исследовательских ноутбуков поверх готового pipeline.

Рекомендуемый принцип:

- код загрузки и очистки живет в `src/`;
- в `notebooks/` лежат только анализ, проверки качества и exploratory work;
- ноутбуки не должны дублировать production-логику из `src/`.
