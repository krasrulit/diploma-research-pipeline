# Notebooks

Папка для ноутбуков, в которых удобно:

- проверять качество распознавания метрик;
- сравнивать значения из DOCX с ручной валидацией;
- готовить исследовательские выборки поверх `raw_long` и `panel_core`.
