# Source Code

Эта папка содержит код парсера отчетов СПАРК.

## Файл

- `spark_docx_parser.py` — основной скрипт для извлечения форм отчетности из `.docx`, сборки `raw_long`, `panel_core`, `coverage` и `parse_log`.
